<%= form_with(model: task, local: true) do |f| %>
<%# ①はここっぽいですねー render 'layouts/error_messages', model: f.object %>

  <%= f.label :status, 'ステータス' %>
  <%= f.text_field :status %>
  
  <%= f.label :content, '内容' %>
  <%= f.text_field :content %>

  <%= f.submit '作成' %>
<% end %>